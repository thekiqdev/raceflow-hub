# ============================================
# PostgreSQL Database Configuration
# ============================================
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=raceflow_db
POSTGRES_USER=raceflow_user
POSTGRES_PASSWORD=raceflow_password

# Database Connection String
# Format: postgresql://user:password@host:port/database
DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@${POSTGRES_HOST}:${POSTGRES_PORT}/${POSTGRES_DB}

# ============================================
# JWT Authentication Configuration
# ============================================
# Generate a strong random string for production
# You can use: openssl rand -base64 32
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXPIRES_IN=7d

# ============================================
# Backend API Configuration
# ============================================
API_PORT=3001
API_URL=http://localhost:3001
NODE_ENV=development

# ============================================
# CORS Configuration
# ============================================
# Comma-separated list of allowed origins
# For development: http://localhost:5173,http://localhost:8080,http://localhost:3000
# For production: https://yourdomain.com,https://www.yourdomain.com
CORS_ORIGIN=http://localhost:5173,http://localhost:8080,http://localhost:3000

# ============================================
# Asaas Payment Gateway Configuration
# ============================================
# Get your API keys from:
# - Sandbox: https://sandbox.asaas.com (for testing)
# - Production: https://www.asaas.com (for production)
# 
# Steps to get API keys:
# 1. Create account at https://sandbox.asaas.com (for testing)
# 2. Go to "Minha Conta" > "Integrações" > "Chaves de API"
# 3. Generate a new API key
# 4. Copy the API key and paste it below
#
# For webhook token:
# 1. Go to "Configurações" > "Webhooks"
# 2. Configure webhook URL (will be set in Etapa 4)
# 3. Copy the webhook token
ASAAS_API_KEY=your_sandbox_api_key_here
ASAAS_WEBHOOK_TOKEN=your_webhook_token_here
ASAAS_ENVIRONMENT=sandbox
ASAAS_API_URL=https://sandbox.asaas.com/api/v3

# For production, change to:
# ASAAS_ENVIRONMENT=production
# ASAAS_API_URL=https://www.asaas.com/api/v3

# ============================================
# Easypanel / Production Configuration
# ============================================
# For Easypanel deployment, update these values:
# - POSTGRES_HOST: Use the PostgreSQL service name (e.g., raceflow-postgres)
# - POSTGRES_PORT: Usually 5432
# - API_URL: Your backend service URL (e.g., https://raceflow-backend-xxxxx.easypanel.host)
# - CORS_ORIGIN: Your frontend domain(s) (e.g., https://raceflow-frontend-xxxxx.easypanel.host)

